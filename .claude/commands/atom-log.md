# Write ATOM Trail Entry

Use the Write-AtomTrail.ps1 function to create a properly formatted ATOM trail entry with:
- Timestamp
- ATOM tag (TYPE-YYYYMMDD-NNN)
- Message

Ask me for:
1. Type (CONFIG, NETWORK, MONITORING, STATUS, etc.)
2. Message

Then write the entry and confirm it was added to ~/.kenl/.atom-trail
