{
  "name": "kenl-api-atom",
  "version": "1.0.0",
  "description": "ATOM Trail Query API Worker",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "test": "echo 'No tests yet' && exit 0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.91.0"
  }
}
